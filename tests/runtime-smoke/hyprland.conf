# Keep the same preferred monitor mode as the preceding experiment.
monitor = , preferred, auto, 1
debug {
    disable_logs = false
}
# A direct exit shortcut; the host launcher then restores GDM.
bind = SUPER SHIFT, E, exit,
